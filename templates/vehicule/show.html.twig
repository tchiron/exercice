{% extends 'base.html.twig' %}

{% block title %}Vehicule{% endblock %}

{% block body %}
    <h1>Vehicule</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ vehicule.id }}</td>
        </tr>
        <tr>
            <th>Immatriculation</th>
            <td>{{ vehicule.immatriculation }}</td>
        </tr>
        <tr>
            <th>Kilométrage</th>
            <td>
                <ul>
                    {% for kilometrage in vehicule.kilometrages %}
                        <li>
                            <table class="table">
                                <tbody>
                                <tr>
                                    <th>Compteur</th>
                                    <th>Date</th>
                                    <th>Utilisateur</th>
                                </tr>
                                <tr>
                                    <td>{{ kilometrage.compteur }}</td>
                                    <td>{{ kilometrage.createdAt ? kilometrage.createdAt|date('Y-m-d') : '' }}</td>
                                    <td>{{ kilometrage.user.email }}</td>
                                </tr>
                                </tbody>
                            </table>
                        </li>
                    {% else %}
                        {{ 'No data' }}
                    {% endfor %}
                </ul>
            </td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vehicule_index') }}">back to list</a>

    <a href="{{ path('app_vehicule_edit', {'id': vehicule.id}) }}">edit</a>

    {{ include('vehicule/_delete_form.html.twig') }}
{% endblock %}
